# v0 (legacy snapshot)

Frozen pre-flake snapshot of the project from when it was named "Lattice".
Names inside `v0/` (hostnames, paths, identifiers) are kept as-is for
historical accuracy. The current project name is **Bravais** — see the
top-level documentation (`README.md`, `BRAVAIS.md`, `CLAUDE.md`).
